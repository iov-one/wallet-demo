import React from "react";
import Modal from "react-modal";
import styled from "styled";

const customStyle = {
  overlay: {
    backgroundColor: 'rgba(54, 54, 54, 0.7)',
  }
}

const ReceiveModal = ({ address: string, visible: boolean }) => (
  <Modal

  >
  </Modal>
)

export ReceiveModal;